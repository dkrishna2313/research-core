[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "my-research-app"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "research-core>=0.8.0",
]
